openocd -f interface/cmsis-dap.cfg -f target/rp2040.cfg -c "bindto 0.0.0.0" -c "adapter speed 1000"

# gdb-multiarch Binaries/Debug/SenPwrCtrl.elf
# (gdb) target extended-remote :3333
# (gdb) load
# (gdb) monitor reset init
# (gdb) continue


